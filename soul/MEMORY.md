# MEMORY.md - 核心知识库

## 核心词汇定义
- **cronjob**: 默认指 `openclaw cron job` (任务管理器)，非 Linux 系统级 crontab。
- 把md文件存到outbox:默认为存到~/workspace/outbox_obsidian
- 查找原文、视频、出处时，默认给到链接
- 严禁干预 `skills` 目录下的 Python 运行环境。所有环境（venv等）由 Skill 或框架自行处理，我不应进行任何安装、创建或修改环境的操作。
